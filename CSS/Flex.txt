display: flex; -- Увімкнути флексовий тип

Задається дочірнім елементам:
flex-basis: 10px 		-- Задати флексовий розмір/ auto - ширина займає розмір контенту бокса
flex-grow: 0			-- 1 - елементи займають всю ширину екрану; 0 - задану
flex-shrink: 0			-- 0 - елементи розтягуються до заданої ширини; 
							1 - елементи зменшуються, до ширини екрану 
							/ auto - ширина займає розмір контенту бокса

							
flex: (flex-grow) (flex-shrink) (flex-basis);

flex-wrap: wrap;		-- переносить елементи у наступний рядок, якщо ті не поміщаються

justify-content: center;	-- Задає розміщення флекс-елему
	*justify-content: center;     /* Выравнивание элементов по центру */
	justify-content: start;      /* Выравнивание элементов в начале */
	justify-content: end;        /* Выравнивание элементов в конце */
	*justify-content: flex-start; /* Выравнивание элементов с самого начала */
	*justify-content: flex-end;   /* Выравнивание элементов с самого конца */
	justify-content: left;       /* Выравнивание элементов по левому краю */
	justify-content: right;      /* Выравнивание элементов по правому краю */у

	 *space-between	-- перший зліва, останній зправа, решта -- рівномірно між ними
https://developer.mozilla.org/ru/docs/Web/CSS/justify-content
	

aligh-items			--Висота блока

	stretch		Елементи розтягуються та займають весь доступний простір контейнера. Без задання.

	center		Елементи розташовані по центру контейнера.

	flex-start	Елементи розташовані на початку контейнера.

	flex-end	Елементи розташовані в кінці контейнера.

	baseline	Елементи розташовані на базовій лінії контейнера.

	initial		Встановлює властивість у значення без задання

	inherit		Вказує на спадковість властивості від свого батьківського елемента (якщо відповідна властивість встановлена)



