```js
const config: EmitterConfigV3 = {
     /**
     * Конфігурація для вибору випадкового числа для тривалості життя кожної частинки.
     */
    lifetime: RandNumber;
   /**
     * Плавність, яка буде застосована до всіх інтерпольованих або ступінчатих значень протягом життя частинки.
     */
    ease?: SimpleEase | EaseSegment[];
    /**
     * Скільки частинок викидати одночасно кожен раз, коли визначено, що частинки мають бути викинуті.
     * Якщо не вказано, викидається лише одна частинка за раз.
     */
    particlesPerWave?: number;
    /**
     * Як часто викидати частинки. Це значення в секундах, тобто значення 0.5 означатиме двічі на секунду.
     */
    frequency: number;
    /**
     * Визначає ймовірність не викидати частинки. Значення менше 1 означають, що частинки можуть не викидатися кожен раз.
     * Якщо не вказано, частинки завжди викидаються.
     */
    spawnChance?: number;
    /**
     * Протягом якого часу працює Emitter, перш ніж він припиняє викидати частинки. Якщо не вказано, то він працює постійно
     * (або до тих пір, поки його не зупинять вручну).
     */
    emitterLifetime?: number;
    /**
     * Максимальна кількість частинок, які можуть бути живими в будь-який момент часу для цього викидача.
     */
    maxParticles?: number;
    /**
     * Чи слід додавати новозроджені частинки до задньої частини батьківського контейнера (щоб вони були менш помітними,
     * коли вони з'являються). Якщо не вказано, частинки будуть додаватися вгору контейнера.
     */
    addAtBack?: boolean;
    /**
     * Позиція за умовчанням для викидання частинок в межах батьківського контейнера.
     */
    pos: { x: number; y: number };
    /**
     * Чи повинен викидач почати відразу викидати частинки. Якщо не вказано, він буде розглядатися як `true` і буде відразу викидати частинки.
     */
    emit?: boolean;
    /**
     * Чи має Emitter підключатися до загального таймера PixiJS. Якщо це false або не вказано, ви будете відповідальні за
     * підключення його до тиків оновлення.
     */
    autoUpdate?: boolean;

    /**
     * Список поведінок, які застосовуються до цього викидача. Див. простір імен behaviors для
     * списку вбудованих поведінок. Власні поведінки можна зареєструвати за допомогою {@link Emitter.registerBehavior}.
     */
    behaviors: BehaviorEntry[];
} 
```
