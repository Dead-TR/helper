const map = new Map(); //- cтворити мап

map.set(ключ, значення); // - додати ключ-значення
map.size; // - дізнатися розмір мапа
map.get(ключ); // - вивести значення ключа
map.has(ключ); // - перевірити наявність ключа
map.delete(ключ); // - видалити ключ-значення


Set -- ті ж самі, крім:

set.add(значення); // - додати значення


{-------------------------------------

		function unique(arr) {
  			return Array.from(new Set(arr));
		}

}-------------------------------------